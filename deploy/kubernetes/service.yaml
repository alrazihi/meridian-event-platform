apiVersion: v1
kind: Service
metadata:
  name: meridian-event-platform
  namespace: meridian
  labels:
    app: meridian-event-platform
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: http
    protocol: TCP
  selector:
    app: meridian-event-platform
